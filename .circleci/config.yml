version: 2
jobs:
  build:
    docker:
      - image: google/cloud-sdk
    steps:
      - run:
          name: rajeshci
          environment:
            GOOGLE_INSTANCE_NAME: onnbazaar-instance
          command: echo $GCLOUD_SERVICE_KEY | base64 --decode --ignore-garbage > ${HOME}/gcloud-service-key.json && cat ${HOME}/gcloud-service-key.json
      - run: |
          gcloud auth activate-service-account --key-file=${HOME}/gcloud-service-key.json
          gcloud --quiet config set project ${GOOGLE_PROJECT_ID}
          gcloud --quiet config set compute/zone ${GOOGLE_COMPUTE_ZONE}
          gcloud --quiet config set compute/instances ${GOOGLE_INSTANCE_NAME}
      - run:
          name: ssh
          environment:
            SSH_KEY: circleci.rsa
            GCP_FOLDER: /var/www/html/demopipeline/
            GCP_PROJECT_INSTANCE: onnbazaar-instance
          command: |
            ssh-keygen -f ${SSH_KEY} -t rsa -N ''
            cat circleci.rsa.pub
            export GCP_FOLDER_PATH=${GCP_FOLDER}
            export SSH_RUN="gcloud compute ssh root@${GCP_PROJECT_INSTANCE} --ssh-key-file=${SSH_KEY}"
            ${SSH_RUN} --command="cd ${GCP_FOLDER_PATH} && git pull"

            